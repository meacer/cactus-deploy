name: cactus

services:
  cactus-init:
    environment:
      CACTUS_CA_ID: "11129.11.99.1"
      CACTUS_LOG_NUMBER: "1"

  sunlight-init:
    entrypoint:
      - /bin/sh
      - -c
      - "sh /docker/init-sunlight.sh && sed -i 's/44363.47.2.1/11129.11.99.2/g' /var/lib/sunlight/sunlight.yaml"
    environment:
      SUNLIGHT_WITNESS_NAME: "sunlight.mirror.test"
      SUNLIGHT_MIRROR_NAME: "oid/1.3.6.1.4.1.11129.11.99.2"

  apache:
    image: httpd:2.4-alpine
    command: >
      sh -c "
        mkdir -p /usr/local/apache2/conf/sites-enabled &&
        sed -i 's/#LoadModule proxy_module/LoadModule proxy_module/' /usr/local/apache2/conf/httpd.conf &&
        sed -i 's/#LoadModule proxy_http_module/LoadModule proxy_http_module/' /usr/local/apache2/conf/httpd.conf &&
        sed -i 's/#LoadModule ssl_module/LoadModule ssl_module/' /usr/local/apache2/conf/httpd.conf &&
        sed -i '/Listen 443/d' /usr/local/apache2/conf/httpd.conf &&
        echo 'Listen 443 https' >> /usr/local/apache2/conf/httpd.conf &&
        echo 'Include conf/extra/apache-docker.conf' >> /usr/local/apache2/conf/httpd.conf &&
        echo 'IncludeOptional conf/sites-enabled/*.conf' >> /usr/local/apache2/conf/httpd.conf &&
        httpd-foreground
      "
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./apache-docker.conf:/usr/local/apache2/conf/extra/apache-docker.conf:ro
      - /etc/letsencrypt:/etc/letsencrypt
      - certbot-webroot:/var/www/certbot:ro
      - ./certs:/etc/certs:ro
      - ./www:/var/www
      - ./sites-enabled:/usr/local/apache2/conf/sites-enabled
    depends_on:
      - cactus
      - skylight

  certbot:
    image: certbot/certbot:latest
    entrypoint: >
      sh -c "
        trap exit TERM;
        while true; do
          certbot renew --webroot -w /var/www/certbot --quiet;
          sleep 5d & wait \$$!;
        done
      "
    volumes:
      - /etc/letsencrypt:/etc/letsencrypt
      - certbot-webroot:/var/www/certbot

volumes:
  certbot-webroot:
